#  Running the app

## Prerequisites
  - Virtual Environment - pip install virtualenv
  - virtualenv NAME OF Environment(env to run app later)
  -  pip install flask

## To run the app 

```sh
$ source bin/activate
$ python main.py 
```
